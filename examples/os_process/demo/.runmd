# Cargo Build

```
add node_title .TEXT really cool node
add input_label .TEXT coolness
add output_label .TEXT really coolness
add enable_input .BOOL true
add enable_attribute .BOOL true
add enable_output .BOOL true
add demo_text .TEXT hello_world

add test   .INT 0
add test_2 .TEXT test value
#
# Example of configuring node editor for Process type 
# 
define command      node
define subcommands  node
edit   command::node     .TEXT cargo
edit   subcommands::node .TEXT build
#
# Enable node editor for section (default closed)
#
add enable_node_editor .BOOL true
```
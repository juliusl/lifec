
``` default runtime
define first  call .symbol second
define second call .symbol third
define third  call
```

``` demo call
define demo runtime .symbol  demo
``` runtime
add project_src .text runtime.runmd
```

``` first call
define first timer .symbol first 
``` timer
add duration .int 5
```

``` second call
define second timer .symbol second
``` timer
add duration .int 5
```

``` third call
define third println .symbol third 
``` println
```  